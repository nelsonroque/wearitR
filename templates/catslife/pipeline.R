library(wearitR)

# `data_path` = directory relative to the RProj (in this case, `data`)
# `config_path` = file path to JSON configuration file (in this case, `config.json`)
run_study_datapipeline(data_path = "data", config_path = "config.json")